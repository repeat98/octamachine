# Research log

Record findings as dated entries. Each entry should state the upstream repository and commit, target firmware version, exact command or capture procedure, observation, and what remains uncertain.

## 2026-09-23 — Initial survey

- octabam provides an Octatrack 1.40C image builder, DSP module model, offline renderer, and ColdFire/DSP emulator paths. Its hardware research and image tooling are useful references; whether its image builder can carry a full Machinedrum OS remains unproven.
- Gearmulator MD/MM is a fork of Gearmulator that runs Machinedrum and Monomachine firmware. It can serve as the behavior and audio reference for a user-supplied ROM.
- MAME's `elektronmono.cpp` is a hardware skeleton marked non-working and no-sound. It may inform hardware questions but cannot validate audio.
- The Machinedrum and Octatrack use different ColdFire generations and different DSP hardware: MAME documents a 5206e plus two DSP56303 chips; octabam documents an MCF5445x plus two DSP5636x cores in a DSP56721. This makes a literal image copy unlikely to boot without mapping and shims. Instruction and memory compatibility have not yet been measured.
- The starting workspace contained local reference checkouts, which were copied into ignored `vendor/` for research. `vendor/octemu` is pinned as a submodule at `87000189418c8ca2026dc047bda220b66802809d`. The local octemu checkout had pre-existing uncommitted SDRAM alias and pacing changes; they are preserved as the separate `patches/octemu/0001-sdram-alias-and-interactive-pace.patch`, while the submodule pin refers to its committed upstream state. No firmware was added to this repository. Compatibility measurements remain open.

## 2026-09-23 — Boot image and board profile

- The checked out Gearmulator MD/MM source identifies Machinedrum OS 1.63 as an 8 MiB image with FNV-64 `33b7c1a9e29f43fd`; the MAME skeleton records CRC-32 `3d552c99` and SHA-1 `a872a2f3527063673d6ea6d3080c4c62ef0cadc1` for its OS 1.63 image.
- The Gearmulator model supplies concrete MCF5206E memory and peripheral addresses. Octemu presently loads an Octatrack MAIN OS at `0x40000400`, so it cannot directly boot a Machinedrum flash image. QEMU's generic AN5206 machine may help test early ColdFire execution, but it lacks the Machinedrum board and dual DSP wiring.
- Source discrepancies need resolution before implementing the board: MAME's map comments place DSP HI08 at `0x00400000`/`0x00500000`, while its prose and Gearmulator use `0x00500000`/`0x00600000`; MAME maps 8 KiB internal SRAM while Gearmulator allocates 64 KiB despite an 8 KiB comment; MAME configures 25.447 MHz while Gearmulator models a 40 MHz CPU clock.
- The ignored `base_firmware/` dump matches the public 8 MiB Machinedrum UW OS 1.63 reference by MAME CRC-32/SHA-1 and Gearmulator FNV-64. Its reset PC is `0x0000000c`; its initial SP is zero. Static bootstrap disassembly shows ColdFire control-register setup before normal stack use. This is not yet a runtime boot trace.
- Added a read-only image auditor and an opt-in Gearmulator bus trace patch for SIM, DSP HI08, and unmapped/peripheral accesses. The patched Gearmulator `mdLib` compiles successfully on the local Apple Silicon host with tests disabled; the runtime trace has not yet been run. Firmware remains ignored and is not included in this repository.

## 2026-09-23 — Clean Gearmulator trace build (WP-01)

- Source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`; recursive sources used for `mdLib`: DSP56300 `1378c43074e6ec22f69f14ed55c21e44c5ccadc1`, AsmJit `3577608cab0bc509f856ebf6e41b2f9d9f71acc4`, mc68k `ace95b3d0a5a332db147244762dda65f9a010b9f`, RmlUi `97bb5921595a0528bd67d61f328885ac19643623`, FreeType `828916527ce4f69af722bce46ce54d289001a0bd`, and dlg `72dfcc858c040c54a6a0b88fcb7e70ee186d3167`.
- Target firmware revision: none used. The parent source was checked out clean at its pin in a temporary detached worktree; the trace patch SHA-256 is `d24558fd0556e21c37eff3a7670d1a076f756224a30bd30fa5ac14f923f50d09`.
- Procedure: applied the patch after `git apply --check`; configured with CMake 4.0.1 and Ninja 1.13.2 on macOS 26.6.2 arm64, Apple Clang 21.0.0, using `BUILD_TESTING=OFF`, JUCE plugins disabled, and other synth targets disabled; ran `cmake --build ... --target mdLib --parallel 4`.
- Observation: CMake configured and Ninja built `mdLib` successfully in 222 steps, producing `libmdLib.a`. The build emitted upstream `-Ofast` deprecation and AsmJit memory-operation warnings. CMake could not detect an Xcode version, and `rclone.conf` was absent; neither was needed for this Ninja library target.
- Octemu prerequisites: `make -C vendor/octemu doctor` found all required and optional tools on the documented macOS host. No octemu setup/QEMU/app build or target firmware execution was run.
- Local source state: the original ignored Gearmulator checkout and nested DSP tree remain modified; the isolated build used clean archive contents at the pinned dependency commits and did not alter those checkouts.
- Limit: this verifies instrumentation compilation only. It does not establish an MD firmware boot, target compatibility, or octemu baseline. See [WP-01 provenance report](reports/WP-01-provenance.md) for the full revision and license inventory.

## 2026-09-23 — Firmware and emulator target profiles (WP-02)

- Firmware/source input: Machinedrum SPS-1UW OS 1.63; local ignored raw `.bin` audited with `python3 scripts/audit_md_image.py --json <local image>`. Observation: the 8 MiB image matches MAME CRC-32 `3d552c99`, MAME SHA-1 `a872a2f3527063673d6ea6d3080c4c62ef0cadc1`, and Gearmulator FNV-64 `33b7c1a9e29f43fd`. The local SHA-256 and firmware bytes remain unshared. The local package also contains a SysEx updater; it was not used or parsed.
- Reference source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`. Its pinned `mdmc.cpp` enables the MKII Port A identification loopback; this is the selected emulator board profile, not evidence of the physical unit's revision. MAME `d7ffd71ed97831b7f995337d6c5bd1bbb03898af` names the image `elektron_sps1-1uw_os1.63.bin`.
- Target source: octemu `87000189418c8ca2026dc047bda220b66802809d` declares an Octatrack MKII MCF54455 board, descriptor v2. Its pinned fetch script defaults to OS 1.40C and pins the distribution archive hash to `370c55a3dad3996b8e4b46400a205066fdaf185ad4d0255a3a3f835060573ff0`.
- Local target input: `vendor/octemu/out/os/main.bin` exists at 1,112,560 bytes, but the source distribution archive is absent. Its local origin/version is therefore unverified; WP-05 must restore or reacquire and verify the expected source package before using it as a baseline.
- Feature scope: the [Elektron OS 1.63 manual](https://www.elektron.se/wp-content/uploads/2024/09/machinedrum_manual_OS1.63.pdf), pp. 2–7 and 35–87, documents machine/effect, sequencer, panel, MIDI, UW sampling, and persistence behavior and lists MKI/MKII differences. These are source-documented expectations, not measured emulator/firmware behavior. The report and fixture metadata record the selected MKII UW profile and deferred variants.
- Limit: no local evidence identifies the physical Machinedrum or Octatrack revision, +Drive/carrier configuration, or recovery path. The target profile is explicitly emulator-only; no emulator boot or hardware test was run. See [WP-02 target profile report](reports/WP-02-target-profile.md).

## 2026-09-23 — Capture and comparison contract (WP-03)

- Implementation: version-1 JSON run manifests plus JSONL event traces record run origin, firmware/source/build identity, initial-state mode, stimuli, clock IDs/units/origins, checkpoint timeout, PC semantics, completeness, caps, drops, and stop reason. The plan declares exact fields and absolute tolerances for each comparison.
- Validation: `scripts/compare_captures.py` reports the first event/field mismatch and returns separate outcomes for equivalent, divergent, missing input, invalid input, incomplete trace, and capture failure. It rejects mismatched comparison plans and initial-state modes; it never treats a partial JSONL record as complete.
- Synthetic verification: `make check` runs ten tests against checked-in and temporary fixture data without firmware: equivalent within the fixture-only tolerance, divergent, all four accepted initial-state modes, missing trace/manifest, duplicate-key rejection, firmware identity enforcement, failed capture, trace cap, and truncated final record.
- Limit: the fixture's `0.1` absolute tolerance is a synthetic boundary-test parameter only. No firmware, emulator, clock mapping, timeout, or behavioral tolerance is established. Actual checkpoint triggers remain to be recorded from measured WP-04/WP-05 runs. See [WP-03 evidence report](reports/WP-03-evidence.md).

## 2026-09-23 — Machinedrum Gearmulator startup baseline (WP-04)

- Source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`; recursive source revisions are recorded in [WP-01](reports/WP-01-provenance.md). Instrumentation patches: trace patch `d24558fd0556e21c37eff3a7670d1a076f756224a30bd30fa5ac14f923f50d09` and baseline/checkpoint patch `0c6521bf6c6adc78a6c1c0a696b8e9848829890d99a4bf65995bb0f35b93780e`.
- Firmware: local ignored Machinedrum SPS-1UW OS 1.63, 8 MiB; it matched public CRC-32 `3d552c99`, SHA-1 `a872a2f3527063673d6ea6d3080c4c62ef0cadc1`, and Gearmulator FNV-64 `33b7c1a9e29f43fd`. Firmware bytes and local SHA-256 remain private.
- Procedure: clean pinned source contents configured with CMake 4.0.1/Ninja 1.13.2 and built with Apple Clang 21.0.0 on macOS 26.6.2 arm64. The panel-readiness driver was run twice per capture wrapper, using fresh blank-flash construction and a separately labeled cached start copied from the cold run. Each child had a 300-second timeout and 12,000,000-record cap in full-private mode. A separate summarized capture generated WP-03 manifests and redacted JSONL event projections using build metadata; the comparator checked the repeated cold and cached pairs. Reproduction syntax and build-metadata example are in the [WP-04 report](reports/WP-04-md-baseline.md).
- Observation: each full-private child run recorded 10,340,278 unfiltered MMIO events across cold and cached instances, without truncation. Per labeled instance: SIM reads/writes 2,591,231/2,005,115; DSP1 HI08 reads/writes 6,336/38,772; DSP2 HI08 reads/writes 28,058/500,627. Two independent runs matched in checkpoint metadata and event order when compared by label, MCU cycle, direction, width, and address. The WP-03 comparator also found the summarized cold pair and cached pair equivalent across 5,630 redacted records per comparison.
- Checkpoints: reset PC/SP were `0x0000000c`/`0`; both DSP boot flags were observed by frame 128; firmware readiness arrived at frame 102,400; blank-flash factory initialization completed by frame 793,800; no-stimulus idle was sampled at frames 837,900 and 573,300 for cold and cached phases. All sampled host, MCU, and DSP clocks advanced across the idle interval.
- Negative observations: a one-event cap returned incomplete, a missing image failed before launch, and a temporary stalled driver timed out as incomplete. `make check` passed nine reference validations and 16 tests; patch-stack clean-apply and already-applied preparation checks passed.
- Inference and limits: these observations establish the Gearmulator model's behavior for this image, not execution on octemu or hardware. Both DSPs appear booted within the first 128-frame block, but their exact intra-block completion order is unresolved. The trace stops at firmware readiness; later initialization/idle is documented with snapshots, not raw MMIO events. No audio parity, target DSP compatibility, or hardware clock mapping is claimed.

## 2026-09-23 — Machinedrum DSP and interface evidence imported from octamad (WP-35)

- Source: octamad branch `machinedrum-phase0`, head `aad3e11` (the user's MIT octabam working copy; local). Reference: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2` with `dsp56kEmu` `1378c43`, instrumented by `patches/gearmulator-md-mm/md-reference/`. Firmware: MD SPS-1UW OS 1.63; update `.syx` SHA-256 `a58cd61f…42cabd5`; the dump matches FNV-64 `33b7c1a9e29f43fd`.
- Procedure (octamad): `md_extract.py` decodes the update and parses both DSP load images and the 135 machine descriptors. `md_profile` boots the dump and profiles both DSPs per JIT block, traces host-port, handler and ESSI traffic, maps parameters to record words and captures the voice DSP. `md_replay` runs the voice DSP's loop standalone from a capture, and `md_relocate.py` moves its code. The tools and commands are in `scripts/md_reference/`.
- Observations:
  - **identities:** section 1 is the voice DSP (Gearmulator DSP2 at `0x600000`), section 2 the mixer (DSP1 at `0x500000`);
  - **external memory:** both programs execute from external RAM at `0x100000+`;
  - **voice DSP work:** peak ~1,650 cycles/sample with 16 voices, the mixer's a constant ~1,850, both of 2,304 (emulator cycle model);
  - **inter-DSP link:** 512-word periods of 32 samples × 16 voices;
  - **standalone replay:** the voice loop outside the MD is bit-identical in six captures covering all 50 core engines, also after relocating its code (987 patched words);
  - **memory:** the voice DSP needs ~71–76 K words plus P-I buffers, and the E12 samples add 201.8 K words;
  - **window fetch:** fetching from the shared window adds ~80 % (fetch-counted).
- Checked in this repository: `md_extract.py` reproduces every pinned section hash, both load maps and the catalog. The patches stack cleanly after `0001` and `0002` on the pinned sources.
- Inference and limits: everything is Gearmulator-only or static; nothing ran on octemu, a DSP5636x model or hardware. The runs predate the WP-03 contract and are prior evidence. The mixer is not relocated, and its host stream is not decoded. The interpreter and JIT builds disagree on two captures, for a reason not found. Details, retractions and adaptation-ledger candidates: [WP-35 report](reports/WP-35-octamad-md-import.md).

## 2026-09-23 — WP-35 c10 voice-DSP replay reproduction

- Source: octamachine's WP-35 reference tools at `fb2c29d07f6d0468944fa5d80c960fb7e387e6ec`; Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`, recursive DSP56300 `1378c43074e6ec22f69f14ed55c21e44c5ccadc1`, and the recursive revisions recorded in the [WP-03 manifest](reports/WP-35-c10-replay-v1.manifest.json). WP-04 trace/checkpoint patches and both WP-35 reference patches were applied to a separate clean source copy.
- Firmware: local MD SPS-1UW OS 1.63, identified by public size/CRC-32/SHA-1/FNV-64 fingerprints. Firmware bytes, DSP payloads, `c10`, and raw logs remain local.
- Procedure: configured Gearmulator with CMake 4.0.1 and Apple Clang 21.0.0 in Release mode on macOS 26.6.2 arm64, built `md_profile`, `md_replay`, and `md_dis` for arm64/x86_64; ran `md_profile <local OS 1.63 image> out/md_profile/cap capture=0x10`; then ran `md_replay out/md_profile/cap/c10` twice.
- Observation: build and profile completed successfully. Both replay runs exited 0 and compared 33,513 blocks of 32 samples with zero differing blocks. The second run's terminal summary is the only event in the WP-03 JSONL trace; the manifest and JSONL validate. The profiler destination directory must exist before invocation.
- Limit: this is one standalone voice-DSP capture in the Gearmulator model. It does not reproduce the six-capture/all-engine or relocated replay results in the original WP-35 report and establishes no Octatrack or physical behavior. See the [follow-up report](reports/WP-35-octamad-md-import.md).

## 2026-09-23 — Octatrack OS 1.40C headless/UI baseline (WP-05)

- Source: octemu `87000189418c8ca2026dc047bda220b66802809d`; QEMU base `e8d693e12af9cbb89d724baadfcc08559669e279` with its 13 committed patch files; DSP56300 base `c051afad31612c2d2c7a81a7ab23e1c5ac9e61af` with its 11 committed patch files. `vendor/octemu/` remains pinned; generated dependency source/build outputs are local to this worktree.
- Firmware: official Octatrack OS 1.40C distribution archive, SHA-256 `370c55a3dad3996b8e4b46400a205066fdaf185ad4d0255a3a3f835060573ff0`; extracted `main.bin` section size 1,112,560 bytes. `make -C vendor/octemu os` verified the archive pin before extraction. The image was not patched or transformed.
- Procedure: `make -C vendor/octemu doctor`, `setup`, `os`, `JOBS=8 make -C vendor/octemu qemu`, `make -C vendor/octemu`, and `make -C vendor/octemu fixtures`. From `vendor/octemu/`, ran the two scripts in `tests/walks/` using `--read-only --os out/os/main.bin`, separate card/NVRAM copies of the same local FX2 fixture, and `--timeout 240`; the headless command added `--headless`, while the windowed command used `OCTA_NO_FOCUS=1` and saved built-in PPM panel screenshots.
- Observation: both bounded runs saw `PTCH`, the loading text disappeared, PLAY satisfied the scripted lamp condition, STOP completed, and the process exited successfully. Headless marks: before PLAY at block 56,105, response at 61,681, stopped at 64,888. Windowed marks: response at 60,944, stopped at 64,274. Both runs reported DSP core ROM boot completion at `0x31000` and `0x32000`. A separate no-card diagnostic reached `C0MPACT` within its 90-second ceiling; this is not the `PTCH` checkpoint.
- Host artifact: the windowed live monitor reported a ratio of `0.8420–1.0000`, 298 cents, and 90,181 starved frames. The source separates this resampled host playback path from guest audio blocks and sample-exact audio recording. No audio was recorded or compared; the host report is not a guest timing or hardware result.
- Evidence: [WP-05 report](reports/WP-05-ot-baseline.md), headless [manifest/events](reports/WP-05-ot-headless-v1.manifest.json), and windowed [manifest/events](reports/WP-05-ot-ui-v1.manifest.json). Full logs, firmware, screenshots, card/NVRAM images, and generated builds remain ignored under local `vendor/octemu/out/` or its dependency build trees.
- Validation: `make check` passed nine reference validations, Python compilation, and 20 tests; `git diff --check` passed; both WP-03 manifests and event projections validated as complete (8 headless and 9 windowed records).
- Limit: this verifies one unmodified OS on the pinned octemu model with one generated saved-project state. The source map describes the emulator implementation. No Machinedrum firmware, physical Octatrack, audio parity, all-control coverage, or recovery path was tested.

## 2026-09-24 — ColdFire execution compatibility (WP-06)

- Source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`, recursive `mc68k` `ace95b3d0a5a332db147244762dda65f9a010b9f`; octemu `87000189418c8ca2026dc047bda220b66802809d`, QEMU base `e8d693e12af9cbb89d724baadfcc08559669e279` plus 13 octemu patches. The target probes use QEMU `an5206` with `m5206` and `cfv4e` CPU models.
- Firmware: local Machinedrum SPS-1UW OS 1.63 matching WP-02's public size/CRC-32/SHA-1/FNV-64 fingerprints; raw firmware remains ignored. The source-startup summary patch `0003-opt-in-coldfire-execution-summary.patch` has SHA-256 `e336bfb11875a95886ba798004e2bb3597659804631ea829b0c7d638d9aaf7f6` and is applied after WP-04 patches 0001/0002.
- Procedure: `patch --dry-run -p1 -d /private/tmp/octamachine-wp04-clean < patches/gearmulator-md-mm/0003-opt-in-coldfire-execution-summary.patch`; rebuild Gearmulator target `mdPanelReadinessFirmwareTest`; run with `GEARMULATOR_MD_EXEC_SUMMARY_LIMIT=100000` and the local OS 1.63 image, while unsetting bus-trace environment variables. The committed patch writes only control names and aggregate instruction counts. Reproduction instructions are in `tests/probes/wp06/README.md`.
- Observation: the source driver exited 0 after its cold and cached readiness checkpoints. The first 100,000 executed instructions contained 10 MOVEC-to-control-register writes (VBR ×2, CACR ×2, ACR0 ×2, ACR1 ×2, RAMBAR ×1, MBAR ×1) and two writes to SR; no counted RTE, TRAP, RESET, STOP, or USP stack-mode instructions occurred in that prefix. The raw trace and driver output remain private.
- Target probe: `python3 tests/probes/wp06/run.py --cc /opt/homebrew/bin/m68k-elf-gcc --qemu /private/tmp/octamachine-md-import/vendor/octemu/vendor/qemu/build/qemu-system-m68k` passed. The two CPU models matched on the tested multiply, stack push/pop, trap/RTE frame, privilege exception, and level-4 interrupt case: level 4 stays pending at SR.I=5 and dispatches once when the mask is lowered to 3. Both QEMU models use a raw unaligned VBR. A target-only EUSP probe distinguishes the MCF54455 manual bit `0x20` from QEMU's internal `0x10`; separate MOVEC probes show ACR writes preceding QEMU's RAMBAR0/MBAR aborts.
- Primary device references: [MCF5206E User's Manual](https://www.nxp.com/docs/en/data-sheet/MCF5206EUM.pdf), §§3.2.4 and 3.3; [ColdFire Family Programmer's Reference Manual](https://www.nxp.com/docs/en/reference-manual/CFPRM.pdf), §§1.5, 3.2, and 11.1.2; [MCF54455 Reference Manual](https://www.nxp.com/docs/en/reference-manual/MCF54455RM.pdf), §§3.2.3 and 6.3.1.
- Failure/limit: an initial EUSP test mask confused QEMU's bit with the manual's bit; it was corrected to test both values. QEMU reset via `system_reset` did not reach the reset-vector entry; pinned `cpu.c` sets PC to zero with a TODO, while `-kernel` bypasses reset and sets ELF entry. The generic AN5206 board exposes a timer interrupt for level-sensitive mask tests but no external edge-sensitive level-7 input. No physical hardware, full instruction census, cache/alignment/atomic behavior, or real register effects were tested. See the [WP-06 report](reports/WP-06-coldfire.md).

## 2026-09-24 — WP-06 target register-map and interrupt-source audit

- Source state: octemu `87000189418c8ca2026dc047bda220b66802809d`, QEMU base `e8d693e12af9cbb89d724baadfcc08559669e279` plus 13 octemu patches; source model Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`.
- Procedure: inspected pinned `target/m68k/helper.c`, `target/m68k/op_helper.c`, `hw/m68k/an5206.c`, `hw/m68k/mcf5206.c`, `hw/m68k/mcf_intc.c`, and `hw/m68k/ot-board.c`; compared the named MOVEC registers to the official MCF5206E and MCF54455 manuals.
- Observation: MCF5206E documents RAMBAR at Rc `0xC04` and MBAR at `0xC0F`; MCF54455 documents one RAMBAR at Rc `0xC05`, ACR0–3/CACR, 1 MiB VBR alignment, supervisor-only control-register access, and non-maskable edge-sensitive level 7. Its core programming model does not list the source MBAR encoding. These are target-facility facts; values/effects of the actual firmware writes remain unrecorded.
- QEMU source observation: `cf_movec_to` handles CACR/ACR/VBR only, leaves ACR effects as TODO, and aborts on the source RAMBAR/MBAR encodings. `m68k_cpu_exec_interrupt` accepts only `SR.I < pending_level`; `m68k_set_irq_level` clears the pending level when lowered. `an5206_init` sets the ELF entry as PC, bypassing reset vectors. The Octatrack board connects modeled devices through two MCF INTC instances, with no separate external level-7 input. Therefore the current pinned machines cannot verify reset-vector fetching or the edge-latched level-7 exception.
- Limit: these source/manual comparisons do not establish real board wiring, firmware register values, or silicon effects. Candidate static rewrite and runtime trap mechanisms were bounded by the target register map and supervisor privilege, but no safe site or adaptation is selected. See the [WP-06 report](reports/WP-06-coldfire.md).

## 2026-09-24 — WP-06 extended ColdFire execution profiles

- Source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`; recursive `mc68k` `ace95b3d0a5a332db147244762dda65f9a010b9f` and DSP56300 `1378c43074e6ec22f69f14ed55c21e44c5ccadc1`. The instrumented profile was built from a separate local source clone, after WP-04 patches `0001`/`0002`, with WP-06 sanitized execution summary patch `0003` and WP-35 host-trace/DSP-execution-hook patches. The existing user Gearmulator checkout was not modified.
- Firmware: local Machinedrum SPS-1UW OS 1.63 image matching WP-02's public size/CRC-32/SHA-1/FNV-64 fingerprints; bytes and local SHA-256 remain private.
- Procedure: rebuilt `mdPanelReadinessFirmwareTest` and the Release/Ninja JIT `md_profile` target; ran the readiness driver with `GEARMULATOR_MD_EXEC_SUMMARY_LIMIT=1000000`, then ran `md_profile <local image> <private output> 0x10` and `trace=0x10` with a 1,000,000,000-instruction counter ceiling. The single-engine profile assigns engine `0x10` to track 1 and triggers it eight times. The trace profile assigns it, triggers it, moves encoder A by +10, and triggers it again. Full reproduction commands and categorized results are in the [WP-06 report](reports/WP-06-coldfire.md).
- Observation: all three processes exited 0. The startup counter reached 1,000,000 instructions and recorded 10 `MOVEC`-to operations (VBR ×2, CACR ×2, ACR0 ×2, ACR1 ×2, RAMBAR ×1, MBAR ×1), two move-to-SR operations, and no counted RTE/TRAP/RESET/STOP/USP-stack instructions. The one-engine profile completed at 562,290,551 instructions (139,161 RTE, 12 TRAP, 1,030,050 move-to-SR, 77,568 move-from-SR). The trace profile completed at 559,651,805 instructions (137,761 RTE, 6 TRAP, 1,013,716 move-to-SR, 77,254 move-from-SR); safe aggregation reported 3,440 transitions into the instrumented descriptor-handler PC range across two handler/return buckets.
- Limit: this is executed behavior in one Gearmulator model and two scenarios around one engine. The counter records aggregate categories only; the detailed trace and address-bearing call rows remain in `/private/tmp`. It does not compare firmware instructions against the Octatrack CPU, establish all-engine coverage, resolve startup operands, or measure physical silicon. Reset fetch and edge-sensitive level 7 remain unresolved as described in WP-06.

## 2026-09-24 — WP-06 runtime profiles across five core engine families

- Source and procedure: the same isolated Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2` profile build and local OS 1.63 image described above. Ran assignment/eight-hit scenarios for TRX-BD `0x10`, TRX-SD `0x11`, EFM-BD `0x20`, and E12-BD `0x30`; ran `trace=<id>` scenarios for GND-SN `0x01`, TRX-BD `0x10`, TRX-SD `0x11`, EFM-BD `0x20`, E12-BD `0x30`, and P-I-BD `0x40`. Each process used a 1,000,000,000-instruction summary ceiling and exited 0.
- Observation: the three added assignment/eight-hit runs completed at 562,231,801 instructions for `0x11`, 562,249,453 for `0x20`, and 562,158,592 for `0x30`. The six traces completed at 559,631,603 (`0x01`), 559,651,805 (`0x10`), 559,752,802 (`0x11`), 559,750,509 (`0x20`), 559,692,448 (`0x30`), and 559,777,600 (`0x40`). Safe aggregation of the private call rows reported 3,440–3,600 entries per scenario, in two handler/return buckets per scenario.
- Limits: these source-model profiles touch five engine families, but aggregate counters do not attribute calls to every descriptor or identify full instruction coverage. The private `.host.txt`, `.link.txt`, `.mem.txt`, DSP cycle profiles, stdout, and address-bearing `.calls.txt` remain local. Nothing ran on the Octatrack CPU or physical hardware. See the [WP-06 report](reports/WP-06-coldfire.md).
- Delivery note: commit `128730dd758cae6f430849bf96daaf0e7743afc5` is pushed to draft PR #12. An attempt to refresh its description through the GitHub integration returned 403 `Resource not accessible by integration`; the branch/report are current and PR metadata awaits a signed-in maintainer.

## 2026-09-24 — WP-06 QEMU data-alignment and byte-order probe

- Probe: added independently authored `tests/probes/wp06/alignment_endian.S` to the `run.py` firmware-free comparison on QEMU `an5206` with `m5206` and `cfv4e`. The synthetic program writes `0x12345678` to aligned RAM, reads a byte plus aligned/odd words and longs, and writes/reads `0xa1b2c3d4` at an odd address. It routes access errors to a fixed failure marker; it uses no firmware-derived inputs.
- Observation: both QEMU CPU models passed with identical values: byte `0x12`; words `0x1234` and `0x3456`; longs `0x12345678` and `0x34567800`; odd-address long store readback `0xa1b2c3d4`. The integrated `python3 tests/probes/wp06/run.py --cc /opt/homebrew/bin/m68k-elf-gcc --qemu /private/tmp/octamachine-md-import/vendor/octemu/vendor/qemu/build/qemu-system-m68k` run also passed the earlier arithmetic, exception, privilege, EUSP, MOVEC, and interrupt checks.
- Limit: this establishes only the pinned QEMU models' behavior for synthetic RAM. It does not establish device-memory access semantics, physical bus behavior, or concurrent atomicity. Atomic instructions, caches, and self-modifying code remain untested. See the [WP-06 report](reports/WP-06-coldfire.md).
- Portfolio extension: separate Gearmulator source profiles ran assignment/eight-hit cases for `0x11`, `0x20`, and `0x30`, plus trigger/encoder traces for `0x01`, `0x10`, `0x11`, `0x20`, `0x30`, and `0x40`. The counters completed below one billion instructions and aggregates are documented in the same report; detailed traces and handler addresses remain local.
- Delivery note: GitHub API access permits reading PR #12 but denied a description edit with 403. The branch/report contain the current evidence; the PR body remains older until metadata write access is available.

## 2026-09-24 — WP-06 runtime profiles across all core engine families

- Source and procedure: the same isolated Gearmulator MD/MM build at `8cea0524a75435122c20b669ca114c9ac6509ba2` and local OS 1.63 image described above. Ran assignment/eight-hit profiles for `0x01`, `0x10`, `0x11`, `0x20`, `0x30`, `0x40`, `0x50`, `0x60`, and `0x70`, plus `trace=<id>` trigger/encoder profiles for the same nine IDs. Each process used a 1,000,000,000-instruction ceiling. Firmware, per-instruction output, and address-bearing trace products remain under `/private/tmp`.
- Observation: all nine assignment/eight-hit runs and all nine trace runs exited 0 below the ceiling. The assignment profiles completed at 562,158,592–562,301,574 instructions. The trace profiles completed at 559,619,278–560,135,090 instructions; aggregate entries into the instrumented descriptor-handler PC range totaled 3,355–3,680 per scenario. Eight traces formed two handler/return buckets and INP-GA `0x50` formed one. The profiles represent GND, TRX, EFM, E12, P-I, INP, MID, and CTR families, with two TRX variants; detailed counters are in the [WP-06 report](reports/WP-06-coldfire.md).
- Limits: these are Gearmulator source-model executions and aggregate handler-range counts. They do not attribute buckets to every descriptor, establish full opcode or 44-handler coverage, compare the firmware instruction stream against CFV4e, or measure the target hardware. The MID `0x60` trace's 16 TRAPs were not separately investigated.
- Delivery note: the updated branch remains draft PR #12. The PR body still describes the earlier profile set because GitHub API metadata edits returned 403.

## 2026-09-24 — WP-06 assignment profiles for all 50 core synthesis IDs

- Source: Gearmulator MD/MM `8cea0524a75435122c20b669ca114c9ac6509ba2`, recursive `mc68k` `ace95b3d0a5a332db147244762dda65f9a010b9f`, and DSP56300 `1378c43074e6ec22f69f14ed55c21e44c5ccadc1`; isolated instrumented build includes the sanitized WP-06 execution summary and WP-35 profiling hooks. The existing user Gearmulator checkout was not modified.
- Firmware: local Machinedrum SPS-1UW OS 1.63 matching WP-02's public size/CRC-32/SHA-1/FNV-64 fingerprints; firmware bytes and SHA-256 remain private.
- Procedure: ran `md_profile <local OS 1.63 image> <private output> <engine ID>` for every core synthesis ID `0x01–0x03`, `0x10–0x1d`, `0x20–0x27`, `0x30–0x3f`, and `0x40–0x48`, with `GEARMULATOR_MD_EXEC_SUMMARY_LIMIT=1000000000`. All 50 IDs were asserted against the local engine catalog. Three auxiliary assignment profiles (`0x50`, `0x60`, `0x70`) and nine trigger/encoder profiles are documented in the [WP-06 report](reports/WP-06-coldfire.md). Firmware, summaries, detailed traces, and address-bearing rows remain under `/private/tmp`.
- Observation: all 50 core runs exited 0 below the ceiling. Executed-instruction totals ranged from 562,087,831 to 562,321,559; RTE counts ranged from 139,028 to 139,497; every run recorded 12 TRAPs. A first `0x12` attempt used a non-instrumented binary and produced no summary; it was discarded, and the instrumented rerun exited 0 at 562,177,565 instructions.
- Limits: these counts describe Gearmulator source-model execution only. They do not compare each firmware instruction with CFV4e, identify full instruction coverage, attribute calls to all 44 distinct descriptor handlers, or establish physical CPU behavior. See the [WP-06 report](reports/WP-06-coldfire.md).

## 2026-09-24 — WP-06 pinned QEMU atomic/cache/code-coherence source audit

- Source: octemu 87000189418c8ca2026dc047bda220b66802809d; exact QEMU base e8d693e12af9cbb89d724baadfcc08559669e279 with all 13 octemu QEMU patches applied in a temporary checkout at /tmp/octamachine-qemu-audit.
- Procedure: initialized the pinned octemu submodule; fetched the exact QEMU base; applied the 13 patch files; inspected target/m68k/cpu.c, target/m68k/helper.c, target/m68k/translate.c, accel/tcg/tb-maint.c, and octemu patch 0008. Confirmed the submodule pointer in the parent checkout stayed unchanged.
- Source observations: neither the m5206 nor cfv4e initializer enables M68K_FEATURE_CAS (the feature is enabled by the distinct 68020 initializer). cf_movec_to stores CACR and handles stack switching but leaves ACR0–3 as TODO. The cache push/invalidate and instruction-touch translator bodies do not model cache operations. TCG tracks translated-code spans and invalidates overlapping blocks after stores; octemu patch 0008 skips only pages without translated code or stores provably outside the tracked span.
- Limit: static source evidence only. This WSL environment has m68k-elf-gcc but no qemu-system-m68k, Meson, Ninja, pkg-config, or built DSP56300 archive, so no new probe was run. The source firmware's use of these instructions and physical CPU/cache behavior remain unknown; no compatibility-matrix conclusion changed.


## 2026-09-24 — WP-06 pinned-QEMU CAS and code-coherence probes

- Source: octemu 87000189418c8ca2026dc047bda220b66802809d; QEMU base e8d693e12af9cbb89d724baadfcc08559669e279 with all 13 octemu QEMU patches; DSP56300 source/dependency archives built by the pinned octemu setup. The parent submodule pointer remained unchanged.
- Build: make -C vendor/octemu setup passed. QEMU was configured for m68k-softmmu with QEMU_EXTRA_CONFIGURE=--disable-werror. Compilation reached step 1,472 of 1,473; the stock final link failed because patch 0011 adds -lc++ while this Ubuntu 26.04 GCC setup lacks that library, and the extracted static GLib/Pixman links need local runtime/transitive dependencies. In the ignored generated build tree only, the Linux link arguments omitted -lc++, added the DSP build's libvtuneSdk.a and -ldl plus GLib's sysprof-capture, pcre2, and atomic dependencies, and resolved Pixman from the installed runtime. Direct Ninja linking passed. No source patch or submodule pin was changed; reproducing the binary with this host needs an equivalent generated-link adjustment.
- Procedure: ran python3 tests/probes/wp06/run.py with m68k-elf-gcc and the resulting qemu-system-m68k binary on the AN5206 QEMU board model. The integrated runner builds independent firmware-free ELFs and reads a fixed synthetic result block. CAS.L is assembled with 68020 syntax; a 68020 control validates the opcode before m5206/cfv4e runs. The code-write probe replaces MOVEQ #1 at an already executed RAM address with MOVEQ #2 and calls it again.
- Observations: the m68020 control changed memory from 0x12345678 to 0x87654321 and returned success. Both m5206 and cfv4e took the illegal-instruction vector (marker 0xbad00004) for CAS and read back the unchanged target word 0x12345678. Both ColdFire models returned 1 on the first code execution and 2 after the replacement write; the runner passed. The binary's machine list includes an5206 and octatrack; ldd reported no unresolved runtime dependencies.
- Limits: these are observations about the pinned QEMU/TCG models only. The CAS result does not establish silicon instruction support or firmware use. The code-write result does not model physical instruction-cache/control-register behavior. Firmware, physical hardware, cache-control effects, reset-vector fetch, external level 7, and device-memory alignment were not tested.

## 2026-09-24 - WP-06 pinned-QEMU cache-control and CACR probe

- Source: octemu 87000189418c8ca2026dc047bda220b66802809d; QEMU base e8d693e12af9cbb89d724baadfcc08559669e279 with all 13 octemu patches.
- Procedure: inspected the pinned CPUSHL translator, ColdFire CACR write helper, and generic CACR read feature gate. Added a firmware-free CPUSHL/CACR probe and ran the integrated WP-06 runner on the AN5206 QEMU machine. The GNU ColdFire assembler rejects CACR-to-D1 MOVEC syntax, so it uses raw words 0x4e7a, 0x1002, verified against GNU as output for -mcpu=68020.
- Observations: m5206 and cfv4e both execute supervisor CPUSHL.L and return from MOVEC D0-to-CACR; both take vector 4 on CACR-to-D1. The trap handler records the unchanged synthetic word 0x13579bdf. QEMU source marks CPUSHL as a no-op, stores CACR writes, and feature-gates CACR reads to 68020/030/040/060 profiles.
- Limits: the result measures only pinned QEMU decoding and helper behavior. It does not observe physical cache state, establish physical CACR readability, or show that Machinedrum firmware uses these paths. No firmware or physical target was used.


## 2026-09-24 — WP-06 pinned-QEMU user-mode cache-control exception probes

- Source: octemu 87000189418c8ca2026dc047bda220b66802809d; QEMU base e8d693e12af9cbb89d724baadfcc08559669e279 with all 13 octemu QEMU patches.
- Procedure: added tests/probes/wp06/user_privilege_cache.S with independent CPUSHL, MOVEC D0-to-CACR, and MOVEC CACR-to-D1 cases. Each enters user mode with SR 0x0500, installs vectors 4 and 8, and records a fixed result block. Ran the integrated WP-06 runner on an5206 with m5206 and cfv4e.
- Observation: both CPU profiles take vector 8 for user-mode CPUSHL and CACR writes and vector 4 for CACR reads. All six runs record one exception, stacked PC equal to the tested instruction, frame SP 0x7ef8 from 0x7f00, and unchanged sentinel 0x13579bdf. Captured frame values are 0x40200500 (vector 8) and 0x40100500 (vector 4).
- Limit: this records only pinned QEMU exception dispatch and its CACR-read feature gate. It does not establish physical exception priority, physical cache effects, or Machinedrum firmware use. No firmware or physical target was used.
