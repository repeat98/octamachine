# Comparison fixtures

Keep only text metadata here. A useful private capture records Machinedrum model and OS, Gearmulator commit, boot checkpoint or user action, sequencer/project state, machine and parameters where relevant, sample rate, gain, command, file hashes, and hardware or emulator origin. Firmware and audio renders are ignored by Git.
