# Image preparation

Future local-only tools for inspecting user-supplied Machinedrum images, applying minimal verified transforms, and preparing an Octatrack boot image belong here. No firmware, derived image, or ROM is committed. Every transform must assert the expected source bytes and emit a report of changed offsets and hashes.
